import React from "react";
import ActionItem from '../ActionItem'

export default () => (
  <footer className="card-footer">
    <ActionItem text="Cargar" />
    <ActionItem text="Editar" />
    <ActionItem text="Borrar" />
  </footer>
);